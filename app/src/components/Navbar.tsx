import { FC } from 'react';
import { AppShell } from '@mantine/core';

const Navbar: FC = () => {
  return (
    <AppShell.Navbar p="md">
      <div>Navbar</div>
    </AppShell.Navbar>
  );
};

export default Navbar;
